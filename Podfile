# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DeviceInfo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

#pod 'DeviceKit'

  # Pods for DeviceInfo

  target 'DeviceInfoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DeviceInfoUITests' do
    # Pods for testing
  end

end
